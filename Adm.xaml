<Window x:Class="Praktika5.Adm"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Praktika5"
        mc:Ignorable="d"
        Title="Adm" Height="450" Width="800" Background="Green">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>


        <DataGrid x:Name="SushiBarHarmony" Margin="10" VerticalAlignment="Stretch" HorizontalAlignment="Stretch"/>
        <Button Content="Выход" Click="ShowMainWindow" Margin="10" HorizontalAlignment="Right" VerticalAlignment="Bottom"/>
        <Button Content="Данные и пароли" Click="Dannie" Margin="10,10,594,357"/>
        <Button Content="Роли" Click="Rols" Margin="206,10,400,357" />
        <Button Content="Сотрудники" Click="Employees" Margin="400,10,204,357"/>
        <Button Content="Поставщики" Click="Sup" Margin="596,10,10,357"/>
    </Grid>
</Window>
